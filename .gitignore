.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Per-drone state that data/ keeps outside git: the live trim and cflib's TOC cache.
data/trim.json
data/hover.json
data/cache/

# Firmware images and recordings, apart from the one backup that documents the drone.
*.bin
!data/firmware-backup.bin
*.csv
